import { EventCategoryPipe } from './event-category.pipe';

describe('EventCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new EventCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
